/** @format */

import { Box, Card, styled } from "@mui/material";

export const DetailProducts = styled(Box)({
  display: "flex",
});

export const DetailLeft = styled(Box)({
  width: 350,
});

export const DetailRight = styled("form")({
  display: "flex",
  flexDirection: "column",
  gap: "1rem",
});
