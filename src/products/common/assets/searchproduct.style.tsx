/** @format */

import { Box, styled } from "@mui/material";

export const SearchProductElement = styled(Box)({
  display: "flex",
  alignItems: "center",
  gap: "1.5rem",
  paddingLeft: 25,
});

export const SearchProductInput = styled(Box)({
  display: "flex",
  gap: 10,
  alignItems: "center",
  border: "1px solid black",
  height: "56px",
  borderRadius: 5,
});
